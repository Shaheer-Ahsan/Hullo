<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/SplashBackground"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".HsplashActivity">

    <ImageView
        android:id="@+id/hulloView"
        android:layout_width="262dp"
        android:layout_height="143dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.556"
        app:srcCompat="@drawable/hullo"
        tools:ignore="ContentDescription" />

    <ImageView
        android:id="@+id/chatView"
        android:layout_width="112dp"
        android:layout_height="24dp"
        android:layout_marginBottom="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        app:layout_constraintBottom_toBottomOf="@+id/hulloView"
        app:layout_constraintEnd_toEndOf="@+id/hulloView"
        app:layout_constraintHorizontal_bias="0.664"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintStart_toStartOf="@+id/hulloView"
        app:layout_constraintTop_toTopOf="@+id/hulloView"
        app:layout_constraintVertical_bias="0.378"
        app:srcCompat="@drawable/chat"
        tools:ignore="ContentDescription" />
</android.support.constraint.ConstraintLayout>